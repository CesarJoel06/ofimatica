// Configuración central (ajusta estos valores para tu despliegue)
window.CFOCAP_CONFIG = {
  // Dominio/URL base donde se publica el sitio (sin slash final).
  // Si lo dejas vacío, se usa automáticamente el dominio actual (location.origin).
  // Ejemplo: "https://midominio"
  BASE_URL: "https://cfocapcertificados.lat/c/D-APYM92",
  // Código de verificación por defecto (no se prellena en el buscador; se mantiene como referencia)
  DEFAULT_CODE: "D-APYM92"
};
